{
  "version": 2,
  "builds": [
    { "src": "frontend/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "https://YOUR_BACKEND_URL/api/$1" },
    { "src": "/(.*)", "dest": "/frontend/$1" }
  ]
}
